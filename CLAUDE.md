# UPSTREAM SOURCE OF TRUTH

Public, unlisted GitHub Pages mirror of a client summary. The canonical upstream auto-loads via the operator's local ~/.claude registry; deliberately not committed here because this repo is public.
